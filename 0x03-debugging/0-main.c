#include "main.h"

/**
 * main - this code tests the function that print if an integer is + or -
 * return: 0 (success)
 */

int main(void)

{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
